<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="primero.list">
      <field name="name">primero list</field>
      <field name="model">primero.primero</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="primero.action_window">
      <field name="name">primero window</field>
      <field name="res_model">primero.primero</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="primero.action_server">
      <field name="name">primero server</field>
      <field name="model_id" ref="model_primero_primero"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="primero" id="primero.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="primero.menu_1" parent="primero.menu_root"/>
    <menuitem name="Menu 2" id="primero.menu_2" parent="primero.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="primero.menu_1_list" parent="primero.menu_1"
              action="primero.action_window"/>
    <menuitem name="Server to list" id="primero" parent="primero.menu_2"
              action="primero.action_server"/>
    -->
  </data>
</odoo>